{
  "name": "neterminai",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run tauri:dev --workspace=@neterminai/desktop",
    "build": "npm run build --workspace=@neterminai/desktop",
    "tauri:build": "npm run tauri:build --workspace=@neterminai/desktop",
    "typecheck": "npm run typecheck --workspace=@neterminai/desktop"
  }
}
