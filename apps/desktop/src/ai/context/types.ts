/** @deprecated Terminal context contracts are Core-owned; use capabilities/terminal. */
export type {
  TerminalConnectionKind as TerminalContextConnectionKind,
  TerminalConnectionMetadata,
  TerminalContextSnapshot,
  TerminalTarget as TerminalContextTarget,
} from "../../capabilities/terminal";
